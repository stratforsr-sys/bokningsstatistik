[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'cd frontend && npm ci',
  'cd backend && npm ci'
]

[phases.build]
cmds = [
  'cd frontend && npm run build',
  'cd backend && npm run build:backend'
]

[start]
cmd = 'cd backend && node dist/index.js'
